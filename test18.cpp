#include<iostream>
using namespace std;

int swap(int x, int y);
int main()
{
    int a = 5, b = 8;
    if(swap(a,b)>5)
    {
        cout<<"enter"<<endl;;
    }
    swap(a,b);
    cout<<"a = "<<a;
    cout<<", b = "<<b<<endl;
}
void swap(int x, int y)
{
  int t = x * y
   return t;
}